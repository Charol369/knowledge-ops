"""Streamlit 前端（Sprint 5 / W6 任务）

跑法：uv run streamlit run frontend/app.py
"""

# TODO W6:
# import streamlit as st
# st.title("KnowledgeOps · 智能知识问答")
# user_q = st.text_input("提问：")
# if user_q:
#     response = requests.post("http://localhost:8000/api/v1/query", json={"question": user_q})
#     st.markdown(response.json()["answer"])
#     st.caption(f"trace: {response.json()['trace_id']}")
